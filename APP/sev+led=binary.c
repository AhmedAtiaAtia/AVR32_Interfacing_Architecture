/*
		for( i = 0 ; i < 256 ; i++ )
		{
			DIO_voidSetPortaValue(i);
			if( i <= 99 )
			{
				SEV_voidSet7SegmentValue(SEV_LEFT , (i % 10));
				SEV_voidSet7SegmentValue(SEV_RIGHT , ( i / 10 ));
			}
			_delay_ms(250);
			DIO_voidSetPortaValue(0);
		}

		 */
		 
